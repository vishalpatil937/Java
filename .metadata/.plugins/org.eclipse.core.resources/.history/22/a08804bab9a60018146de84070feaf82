package Test;

public class Dictionary {
	String words[]=new String[5];
	String meaning[]= new String[5];
	int Map[]= new int[5];

	public boolean contains(String[] array,String item) {
		for(String ite:array) {
			if(ite==item || ite.equals(item))
				return true;
		}
		return false;
	}
	
	public String Add(String word,String meaning) {
		int map;
		if(contains(this.words,word)) {
			return "Exists";
		}
		else {
			this.words[this.words.length]=word;
			map=this.meaning.length;
			this.meaning[this.meaning.length]=meaning;
		}
		return "Added";
	}
}
